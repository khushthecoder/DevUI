name: Deploy to Vercel (v2)
env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  vercel-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Vercel CLI
        run: npm install -g vercel@latest
      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=preview --token=$VERCEL_TOKEN
      - name: Build Project Artifacts
        run: vercel build --token=$VERCEL_TOKEN
      - name: Deploy Preview to Vercel
        id: vercel
        run: |
          DEPLOY_URL=$(vercel --prebuilt --token=$VERCEL_TOKEN --yes --scope ${{ env.VERCEL_ORG_ID }} --prebuilt)
          echo "preview_url=$DEPLOY_URL" >> $GITHUB_OUTPUT

      - name: Show Preview URL
        run: echo "Preview URL- ${{ steps.vercel.outputs.preview_url }}"

      - name: Comment Preview URL on PR
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            🚀 Vercel Preview Deployed!
            Preview URL: [${{ steps.vercel.outputs.preview_url }}](${{ steps.vercel.outputs.preview_url }})